/// @description Insert description here
// You can write your code in this editor

if (!surface_exists(renderTarget)) { 
	surface_free(renderTarget)
}
	
if (!surface_exists(renderLayer)) { 
	surface_free(renderLayer)
}